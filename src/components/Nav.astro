---
import { navLinks, services, areas, business } from "../data/site";
import GoogleReviewBadge from "./GoogleReviewBadge.astro";
import Icon from "./Icon.astro";

const currentPath = Astro.url.pathname.replace(/\/$/, "") || "/";
const isActive = (href: string) => (href === "/" ? currentPath === "/" : currentPath === href);
const navLogo = "/images/6949b04c9133021b589bca73_963c31600ecb4833cfe46fa8406c98e1-32bits-256.png";
---

<header class="nav surface-night" data-nav>
  <div class="nav__bar container">
    <a href="/" class="nav__logo" aria-label="Pine Valley Plumbing — home">
      <img src={navLogo} width="256" height="138" alt="Pine Valley Plumbing" />
    </a>

    <GoogleReviewBadge class="nav__badge" />

    <button class="nav__toggle" aria-expanded="false" aria-controls="nav-menu" aria-label="Toggle menu" data-nav-toggle>
      <span></span><span></span><span></span>
    </button>

    <nav id="nav-menu" class="nav__menu" aria-label="Main navigation">
      <ul class="nav__links">
        {navLinks.map((link) => (
          <li>
            <a href={link.href} class="nav__link" aria-current={isActive(link.href) ? "page" : undefined}>
              {link.label}
            </a>
          </li>
        ))}

        <li class="nav__dropdown">
          <button type="button" class="nav__dropbtn" aria-expanded="false" data-dropdown>
            Services <Icon name="chevron-down" class="nav__chev" />
          </button>
          <ul class="nav__dropmenu">
            {services.map((s) => (
              <li><a href={s.href} class="nav__droplink">{s.label}</a></li>
            ))}
          </ul>
        </li>

        <li class="nav__dropdown">
          <button type="button" class="nav__dropbtn" aria-expanded="false" data-dropdown>
            Areas <Icon name="chevron-down" class="nav__chev" />
          </button>
          <ul class="nav__dropmenu">
            {areas.map((a) => (
              <li><a href={a.href} class="nav__droplink">{a.label}</a></li>
            ))}
          </ul>
        </li>
      </ul>

      <div class="nav__buttons">
        <a href={business.bookingUrl} class="btn btn--gold btn--sm">
          <Icon name="calendar" /> Book Now
        </a>
        <a href={business.navPhoneTel} class="nav__phonebox">
          <span class="nav__phonebox-num"><Icon name="phone" /> {business.navPhone}</span>
          <span class="nav__phonebox-sub">24/7 Emergency Service</span>
        </a>
      </div>
    </nav>
  </div>
</header>

<style>
  .nav {
    position: sticky;
    top: 0;
    z-index: 100;
    border-bottom: 2px solid var(--gold-500);
    box-shadow: var(--shadow-night);
  }
  .nav__bar {
    display: flex;
    align-items: center;
    gap: 1rem;
    min-height: var(--header-h);
    padding-block: 0.6rem;
    /* the bar carries more than the 80rem container can hold — span the viewport */
    max-width: none;
  }
  .nav__logo { flex: none; display: inline-flex; }
  .nav__logo img { height: 56px; width: auto; max-width: none; }
  /* :global — the badge root lives in the child component, outside Nav's style scope */
  :global(.nav__badge) { margin-right: auto; }

  /* the full bar (logo + badge + 7 links + 2 CTAs) only fits on wide screens:
     drop the badge first, then tighten links, then hand off to the hamburger */
  @media (max-width: 1649px) {
    :global(.nav .nav__bar .nav__badge) { display: none; }
    .nav__logo { margin-right: auto; }
    .nav__bar { gap: 0.75rem; }
    .nav__menu { gap: 0.9rem; }
    .nav__links { gap: 0.1rem; }
    .nav__link,
    .nav__dropbtn { font-size: 0.85rem; letter-spacing: 0.02em; padding: 0.5rem 0.4rem; }
    .nav__phonebox { padding-inline: 0.7rem; }
  }

  .nav__toggle {
    display: none;
    flex-direction: column;
    justify-content: center;
    gap: 5px;
    width: 44px;
    height: 44px;
    background: transparent;
    border: 0;
    padding: 8px;
    margin-left: auto;
  }
  .nav__toggle span {
    display: block;
    height: 3px;
    border-radius: 3px;
    background: var(--cream-100);
    transition: transform 0.2s ease, opacity 0.2s ease;
  }

  .nav__menu {
    display: flex;
    align-items: center;
    gap: 1.5rem;
  }
  .nav__links {
    display: flex;
    align-items: center;
    gap: 0.35rem;
    list-style: none;
    margin: 0;
  }
  .nav__link,
  .nav__dropbtn {
    display: inline-flex;
    align-items: center;
    gap: 0.3rem;
    background: transparent;
    border: 0;
    color: var(--cream-100);
    font-family: var(--font-body);
    font-weight: 700;
    font-size: 0.9rem;
    text-transform: uppercase;
    letter-spacing: 0.04em;
    padding: 0.5rem 0.7rem;
    border-radius: var(--radius-sm);
    white-space: nowrap;
  }
  .nav__link:hover,
  .nav__dropbtn:hover,
  .nav__link[aria-current="page"] {
    color: var(--gold-500);
  }
  .nav__chev { width: 0.9em; height: 0.9em; transition: transform 0.2s ease; }

  .nav__dropdown { position: relative; }
  .nav__dropmenu {
    list-style: none;
    margin: 0;
    display: none;
    position: absolute;
    top: calc(100% + 6px);
    left: 0;
    min-width: 260px;
    background: var(--night-900);
    border: 1px solid #ffffff1a;
    border-top: 3px solid var(--gold-500);
    border-radius: var(--radius-md);
    box-shadow: var(--shadow-hover);
    padding: 0.4rem;
    z-index: 20;
  }
  .nav__droplink {
    display: block;
    padding: 0.6rem 0.8rem;
    border-radius: var(--radius-sm);
    color: var(--on-forest-body);
    font-weight: 600;
    font-size: 0.9rem;
  }
  .nav__droplink:hover { background: #ffffff14; color: var(--gold-500); }

  /* open states */
  .nav__dropbtn[aria-expanded="true"] + .nav__dropmenu { display: block; }
  .nav__dropbtn[aria-expanded="true"] .nav__chev { transform: rotate(180deg); }

  .nav__buttons { display: flex; align-items: center; gap: 0.7rem; }

  .nav__phonebox {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.1rem;
    padding: 0.4rem 1rem;
    border: 1.5px solid var(--border-gold-soft);
    border-radius: var(--radius-md);
    text-align: center;
    transition: border-color 0.18s ease, background-color 0.18s ease;
  }
  .nav__phonebox:hover { border-color: var(--gold-500); background: #ffffff0a; }
  .nav__phonebox-num {
    display: inline-flex;
    align-items: center;
    gap: 0.45rem;
    font-weight: 800;
    font-size: 1rem;
    color: var(--on-night-strong);
    white-space: nowrap;
  }
  .nav__phonebox-num svg { width: 1em; height: 1em; color: var(--gold-500); }
  .nav__phonebox-sub {
    font-size: 0.62rem;
    font-weight: 800;
    letter-spacing: 0.13em;
    text-transform: uppercase;
    color: var(--gold-500);
    white-space: nowrap;
  }

  /* desktop hover open (mouse enhancement) */
  @media (min-width: 1280px) and (hover: hover) {
    .nav__dropdown:hover .nav__dropmenu { display: block; }
    .nav__dropdown:hover .nav__chev { transform: rotate(180deg); }
  }

  /* ---- mobile / mid-desktop hamburger ---- */
  @media (max-width: 1279px) {
    .nav__toggle { display: flex; }
    .nav__menu {
      position: fixed;
      inset: var(--header-h) 0 0 0;
      flex-direction: column;
      align-items: stretch;
      gap: 0;
      background: var(--night-950);
      padding: 1.25rem var(--gutter) 2.5rem;
      overflow-y: auto;
      transform: translateX(100%);
      transition: transform 0.25s ease;
      border-top: 3px solid var(--gold-500);
    }
    [data-nav][data-open] .nav__menu { transform: translateX(0); }
    [data-nav][data-open] .nav__toggle span:nth-child(1) { transform: translateY(8px) rotate(45deg); }
    [data-nav][data-open] .nav__toggle span:nth-child(2) { opacity: 0; }
    [data-nav][data-open] .nav__toggle span:nth-child(3) { transform: translateY(-8px) rotate(-45deg); }

    .nav__links { flex-direction: column; align-items: stretch; gap: 0; width: 100%; }
    .nav__link, .nav__dropbtn {
      width: 100%;
      justify-content: space-between;
      font-size: 1.05rem;
      padding: 0.9rem 0.25rem;
      border-bottom: 1px solid #ffffff14;
    }
    .nav__dropmenu {
      position: static;
      display: none;
      min-width: 0;
      box-shadow: none;
      border: 0;
      border-radius: 0;
      background: #00000026;
      margin: 0.25rem 0 0.5rem;
    }
    .nav__buttons { flex-direction: column; align-items: stretch; margin-top: 1.5rem; }
    .nav__buttons .btn { width: 100%; }
    .nav__phonebox { width: 100%; padding-block: 0.6rem; }
  }
</style>

<script>
  const root = document.querySelector<HTMLElement>("[data-nav]");
  const toggle = document.querySelector<HTMLButtonElement>("[data-nav-toggle]");
  const dropBtns = Array.from(document.querySelectorAll<HTMLButtonElement>("[data-dropdown]"));

  const isMobile = () => window.matchMedia("(max-width: 1279px)").matches;

  toggle?.addEventListener("click", () => {
    const open = root?.toggleAttribute("data-open");
    toggle.setAttribute("aria-expanded", String(!!open));
  });

  const closeAllDrops = () => dropBtns.forEach((b) => b.setAttribute("aria-expanded", "false"));

  dropBtns.forEach((btn) => {
    btn.addEventListener("click", (e) => {
      e.stopPropagation();
      const open = btn.getAttribute("aria-expanded") === "true";
      closeAllDrops();
      btn.setAttribute("aria-expanded", String(!open));
    });
  });

  // close dropdowns when clicking outside
  document.addEventListener("click", (e) => {
    if (!(e.target as HTMLElement)?.closest?.("[data-dropdown]")) closeAllDrops();
  });

  // Esc closes everything
  document.addEventListener("keydown", (e) => {
    if (e.key === "Escape") {
      closeAllDrops();
      if (root?.hasAttribute("data-open")) {
        root.removeAttribute("data-open");
        toggle?.setAttribute("aria-expanded", "false");
      }
    }
  });

  // close mobile menu after following a link
  document.querySelectorAll<HTMLAnchorElement>(".nav__link, .nav__droplink").forEach((a) => {
    a.addEventListener("click", () => {
      if (isMobile()) {
        root?.removeAttribute("data-open");
        toggle?.setAttribute("aria-expanded", "false");
      }
    });
  });
</script>
