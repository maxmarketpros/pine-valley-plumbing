[build]
  command = "npm run build"
  publish = "dist"

# Site images are fingerprint-free but effectively immutable; cache hard.
[[headers]]
  for = "/images/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"
